apiVersion: v1
kind: Service
metadata:
  name: fava
  annotations:
    tailscale.com/hostname: fava
spec:
  selector:
    app: fava
  type: LoadBalancer
  loadBalancerClass: tailscale
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
